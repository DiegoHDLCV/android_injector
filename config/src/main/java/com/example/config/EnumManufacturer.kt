package com.example.config

enum class EnumManufacturer {
    NEWPOS,
    AISINO,
    INGENICO, // Ejemplo para futura expansión
    PAX,      // Ejemplo para futura expansión
    UNKNOWN   // Para el DummyManager o casos no soportados
}